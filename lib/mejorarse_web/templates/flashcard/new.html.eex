<h1>New Flashcard</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.user_flashcard_path(@conn, :create, @current_user)) %>

<span><%= link "Back", to: Routes.user_flashcard_path(@conn, :index, @current_user) %></span>
